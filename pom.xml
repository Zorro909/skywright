<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>dev.skywright</groupId>
  <artifactId>skywright-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>Skywright reactor</name>
  <description>Aggregator and shared build parent for Skywright project parts.</description>

  <modules>
    <module>backend</module>
  </modules>

  <properties>
    <java.version>25</java.version>
    <maven.compiler.release>${java.version}</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <spring-boot.version>4.1.0</spring-boot.version>

    <maven.clean.version>3.5.0</maven.clean.version>
    <maven.compiler.version>3.15.0</maven.compiler.version>
    <maven.deploy.version>3.1.4</maven.deploy.version>
    <maven.enforcer.version>3.6.3</maven.enforcer.version>
    <maven.failsafe.version>3.5.6</maven.failsafe.version>
    <maven.install.version>3.1.4</maven.install.version>
    <maven.jar.version>3.5.1</maven.jar.version>
    <maven.resources.version>3.5.0</maven.resources.version>
    <maven.surefire.version>3.5.6</maven.surefire.version>
    <maven.toolchains.version>3.3.0</maven.toolchains.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-clean-plugin</artifactId>
          <version>${maven.clean.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${maven.resources.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven.compiler.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven.surefire.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${maven.failsafe.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven.jar.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-install-plugin</artifactId>
          <version>${maven.install.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>${maven.deploy.version}</version>
        </plugin>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-toolchains-plugin</artifactId>
        <version>${maven.toolchains.version}</version>
        <executions>
          <execution>
            <id>select-supported-jdk</id>
            <goals>
              <goal>select-jdk-toolchain</goal>
            </goals>
            <configuration>
              <version>[25.0.4,25.0.5)</version>
              <runtimeVersion>25.0.4+7-jvmci-25.2-b20</runtimeVersion>
              <vendor>GraalVM Community</vendor>
              <useJdk>Never</useJdk>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <version>${maven.enforcer.version}</version>
        <executions>
          <execution>
            <id>enforce-supported-build-runtime</id>
            <phase>validate</phase>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[3.9.16]</version>
                  <message>Build Skywright with the checked-in Maven Wrapper (Maven 3.9.16).</message>
                </requireMavenVersion>
                <requireJavaVersion>
                  <version>[25.0.4,25.0.5)</version>
                  <message>Build Skywright with OpenJDK 25.0.4 from GraalVM Community 25.2.4.</message>
                </requireJavaVersion>
                <requireJavaVendor>
                  <includes>
                    <include>GraalVM Community</include>
                  </includes>
                  <message>Build Skywright with GraalVM Community 25.2.4.</message>
                </requireJavaVendor>
                <requireProperty>
                  <property>java.vendor.version</property>
                  <regex>GraalVM CE 25\.2\.4\+7\.1</regex>
                  <message>Build Skywright with GraalVM Community 25.2.4.</message>
                </requireProperty>
                <requireProperty>
                  <property>java.runtime.version</property>
                  <regex>25\.0\.4\+7-jvmci-25\.2-b20</regex>
                  <message>Build Skywright with the supported OpenJDK 25.0.4+7 runtime.</message>
                </requireProperty>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
